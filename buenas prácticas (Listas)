🔰 Ejercicio 1: Números pares al cuadrado
📌 Crear una lista con los cuadrados de los números pares entre 1 y 20.
cuadrados = [i**2 for i in range(1, 21) if i % 2 == 0] (corregido)✅
print(cuadrados)

🔰 Ejercicio 2: Longitud de palabras
📌 Dada una lista de palabras, crear un diccionario que tenga como clave la palabra y como valor su longitud.
palabras = ["python", "facultad", "back"]
longitud = for palabras in lista count

corregido:
palabras = ["python", "facultad", "back"]
longitud = {palabra: len(palabra) for palabra in palabras}
print(longitud)


😈 Bonus Crack
Convertí esta lógica a comprensión de diccionarios:
""" palabras = ["python", "facultad", "back"]
longitudes = {palabra: len(palabra) for palabra in palabras}
"""
def contar_palabras(lista):
    count = {}
    for palabras in lista:
        count[palabras] = len(palabras)
        
    return count

print(contar_palabras(["python", "facultad", "back"]))

🚀 Parte 1: Comprensión intermedia (listas + diccionarios)
🧠 Ejercicio 1: Filtrar y elevar al cubo
📌 Crea una lista por comprensión con los cubos de los números entre 1 y 30 que sean múltiplos de 3 pero no de 5.
alcubo = [i**3 for i in range(1, 31) if i % 3 == 0 and i % 5 != 0]
print(alcubo)

🧠 Ejercicio 2: Crear diccionario de edades por comprensión
📌 Dada esta lista de personas, generá un diccionario que tenga como clave el nombre y como valor la edad.

person = [("Mili", 22), ("Juan", 25), ("Lu", 30), ("Pedro", 25)]
print(dict(person))

_Por compresión_
edades = {name: age for name, age in person}
print(edades)



